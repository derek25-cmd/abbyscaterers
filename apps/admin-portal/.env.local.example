# Copy to .env.local and fill in real values. Never commit .env.local.

# Clerk (same Clerk application as used for the "supabase" JWT template)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Same Supabase project as apps/catering-system — one project, two auth front-ends.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
